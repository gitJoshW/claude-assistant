# ─────────────────────────────────────────────────────────
# Claude Assistant Phase 3 — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git — add it to .gitignore
# ─────────────────────────────────────────────────────────

# Your Anthropic API key (from console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# A secret string you invent — used to authenticate requests
# from your browser app to this server. Make it long and random.
# Example: openssl rand -hex 32
API_SECRET=your-random-secret-here

# ── EMAIL NOTIFICATIONS ───────────────────────────────────
# The server sends notifications via email using Gmail.
# Use a Gmail account + an App Password (not your real password).
# Generate an App Password at: myaccount.google.com/apppasswords
GMAIL_USER=you@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# The email address to SEND notifications TO (can be same as above,
# or your phone's SMS email gateway for text-style alerts)
NOTIFY_EMAIL=you@gmail.com

# ── NOTIFICATION SCHEDULE (cron syntax) ──────────────────
# Format: "minute hour * * *"
# Examples:
#   "0 8 * * *"     = 8:00am every day
#   "0 8 * * 1-5"   = 8:00am weekdays only
#   "*/30 * * * *"  = every 30 minutes

# Morning briefing time
MORNING_BRIEFING_CRON=0 8 * * *

# Due/overdue check (runs every 2 hours during work hours)
DUE_CHECK_CRON=0 9-17 * * *

# Focus/distraction reminder (runs every 90 min during work hours)
FOCUS_REMINDER_CRON=30 9-17/1 * * *

# Your timezone (for correct scheduling)
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# Port (Render sets this automatically, leave as-is)
PORT=3000
